{
  "type": "postgres",
  "host": "localhost",
  "username": "docker",
  "password": "ignite",
  "database": "rentalx",
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
